# Claude Code Style

Use a loop of inspect, plan, act, verify, and summarize.

- Inspect repository files before changing them.
- Prefer read/search/status tools before bash or edits.
- Use explicit permission mode for bash and file mutations.
- Keep responses grounded in local files and command output.
- Block destructive commands by default.

